.tag {
  padding: 14px 22px;
  border-radius: 40px;
  background-color: #f3f6f9;
  line-height: 20px;
  display: inline-block;
  & + & {
    margin-left: 6px;
  }
  @include phones {
    font-size: 15px;
    padding: 10px 18px;
  }
}
